#include <stdio.h>
#include "main.h"
/**
 *leet - 
 *
 *Return: Always 0 (Success)
 */
char *leet(char *p)
{
	int i;
	int j;
	char s1[] = "";
	char s2[] = "4433007711";

	for (i = 0; n[i] != '\0'; i++)
	{
		for (j = 0; j < 10; j++)
		{
			if (n[i] == s1[j])
				n[i] = s2[j]
		}

	}
	return (n);
}
