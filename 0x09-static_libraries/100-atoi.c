#include <stdio.h>
#include <stdlib.h>
/**
 *_atoi - converts string to integer
 *
 *@s: string to be converted
 *Return: s (Success)
 */
int _atoi(char *s)
{
	int num;

	num = atoi(s);

	return (num);
}
