#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 *_strlen - returns the length of a string
 *
 *@s: a string
 *Return: len (Success)
 */
int _strlen(char *s)
{

	int len;

	len = strlen(s);

	return (len);
}
